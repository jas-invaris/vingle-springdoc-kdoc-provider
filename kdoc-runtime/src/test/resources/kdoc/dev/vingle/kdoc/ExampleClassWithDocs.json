{
  "name": "dev.vingle.kdoc.ExampleClassWithDocs",
  "comment": {
    "text": "This is a data class with some documentation comments for testing.\n\nA JSON was generated with the processor and put into the test resources, so that it's available in the same resource\npath as it would be in practice and can therefore be used by the tests."
  },
  "methods": [
    {
      "name": "methodWithoutParameters",
      "paramTypes": [],
      "comment": {
        "text": "A documented method that doesn't have any parameters."
      },
      "returns": {
        "text": "Does not return anything because it throws an error beforehand."
      },
      "throws": [
        {
          "name": "NotImplementedError",
          "comment": { "text": "This method always raises an error." }
        }
      ],
      "seeAlso": [{ "link": "methodWithParameters" }]
    },
    {
      "name": "methodWithParameters",
      "paramTypes": ["String"],
      "comment": { "text": "A documented method that *does* have parameters." },
      "params": [
        {
          "name": "textParameter",
          "comment": { "text": "A parameter that's also documented." }
        }
      ],
      "other": [{ "name": "inline", "comment": { "text": "" } }]
    },
    {
      "name": "component1",
      "paramTypes": [],
      "comment": { "text": "This is an integer field." }
    },
    {
      "name": "component2",
      "paramTypes": [],
      "comment": { "text": "This is a text field." }
    },
    {
      "name": "copy",
      "paramTypes": ["Int", "String"],
      "comment": { "text": "" }
    },
    {
      "name": "equals",
      "paramTypes": ["Any"],
      "comment": {
        "text": "This is a data class with some documentation comments for testing.\n\nA JSON was generated with the processor and put into the test resources, so that it's available in the same resource\npath as it would be in practice and can therefore be used by the tests."
      },
      "other": [
        {
          "name": "constructor",
          "comment": {
            "text": "The constructor can be used to instantiate an object."
          }
        }
      ]
    },
    {
      "name": "hashCode",
      "paramTypes": [],
      "comment": {
        "text": "This is a data class with some documentation comments for testing.\n\nA JSON was generated with the processor and put into the test resources, so that it's available in the same resource\npath as it would be in practice and can therefore be used by the tests."
      },
      "other": [
        {
          "name": "constructor",
          "comment": {
            "text": "The constructor can be used to instantiate an object."
          }
        }
      ]
    },
    {
      "name": "toString",
      "paramTypes": [],
      "comment": {
        "text": "This is a data class with some documentation comments for testing.\n\nA JSON was generated with the processor and put into the test resources, so that it's available in the same resource\npath as it would be in practice and can therefore be used by the tests."
      },
      "other": [
        {
          "name": "constructor",
          "comment": {
            "text": "The constructor can be used to instantiate an object."
          }
        }
      ]
    },
    {
      "name": "<init>",
      "paramTypes": ["Int", "String"],
      "comment": { "text": "" }
    }
  ],
  "constructors": [
    {
      "name": "<init>",
      "paramTypes": ["Int", "String"],
      "comment": { "text": "" },
      "isConstructor": true
    }
  ],
  "other": [
    {
      "name": "constructor",
      "comment": {
        "text": "The constructor can be used to instantiate an object."
      }
    }
  ],
  "fields": [
    { "name": "number", "comment": { "text": "This is an integer field." } },
    { "name": "text", "comment": { "text": "This is a text field." } }
  ]
}
